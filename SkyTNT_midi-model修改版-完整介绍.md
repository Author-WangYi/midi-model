# midi-model 便携版 · 使用说明

> 用 AI 生成 MIDI 音乐的本地工具 —— 免安装、免配置、内置模型、离线可用

**免安装** 解压就能跑 ｜ **免配置** 不用装 Python ｜ **内置模型** 启动不联网 ｜ **可搬迁** 改名换盘都行 ｜ **本地运行** 数据不出电脑

---

## 一、这是什么

一个跑在自己电脑上的 AI 作曲工具。你说要什么风格，它把音符一个个"写"出来，最后给你一份能直接用的 MIDI 文件。

| 项目 | 说明 |
|---|---|
| 软件名称 | Midi-Model（作者 SkyTNT，开源项目） |
| 它做什么 | 生成 MIDI 音乐。核心是一个约 12 层的 Transformer 模型（LLaMA 架构），把 MIDI 里的音符、力度、乐器、速度等信息拆成 3406 种「词」，像语言模型写文章那样一个字一个字往下预测，拼成一段完整的曲子 |
| 怎么用 | 双击启动后，用浏览器打开一个本地网页界面，填好要求点生成，等几秒到几十秒 |
| 给什么 | **自定义提示**：指定乐器、鼓组、BPM、拍号、调号，让它从零开始编<br>**MIDI 续写**：丢一个 MIDI 文件进去，它顺着往下写<br>**接着上次**：在上一次的结果上继续延伸 |
| 出什么 | 同时给你三样：MIDI 文件（可下载）、WAV 音频（可试听）、网页里滚动的乐谱可视化 |
| 一次几个 | 默认一次生成 **8 个方案**，挑选满意的那个 |
| 跑在哪 | 完全在你自己的电脑上运算，不上传任何东西，也不用注册账号 |

---

## 二、原版为什么不能直接双击，我改了什么

作者发布的是"程序本体"，不是一个"成品软件"。解压后直接双击，通常只会看到一个黑窗口一闪而过。

### 原版：卡在三道门槛上

| 门槛 | 问题 |
|---|---|
| **门槛一：要自己装 fluidsynth** | 这是合成声音用的底层库，官方只提供源码，得自己找 Windows 编译版，装完还要配环境变量 |
| **门槛二：程序把路径写死了** | 程序内部固定去 `C:\tools\fluidsynth\bin` 找它。装在别的位置？直接崩溃退出，报 `FileNotFoundError` |
| **门槛三：模型要联网现下** | 第一次运行才去 HuggingFace 下载约 **990 MB** 的模型和音色库。国内网络下这一步经常超时失败，看起来就像"卡住了" |

### 便携版：这些都替你处理好了

- **fluidsynth 直接打包进文件夹** —— 6 MB 的库文件已经躺在包里，不用你下载，也不用管环境变量。
- **用"自愈式启动器"绕过硬编码** —— 每次启动自动在 C 盘维护一个几 KB 的目录链接，让程序以为库在它指定的位置。文件夹**改名、换盘、换电脑**都不用重配，启动器会自己修好。
- **模型和音色库预先放好** —— 990 MB 的模型已随包提供，**启动和生成都不需要联网**。
- **额外清理：删掉了约 3 GB 的无用依赖** —— 官方包里混入了一套用不上的深度学习运行库（PyTorch）。实测确认这个程序实际用的是 ONNX Runtime 推理，于是把这堆 3 GB 的累赘清了出去。

### 使用体验对照

| 环节 | 原版 | 便携版 |
|---|---|---|
| 环境准备 | 自己下载、安装、配置 fluidsynth | **零配置**，库已在包内 |
| 存放位置 | 受限于写死的 C 盘路径 | **任意盘、任意文件夹名**，含空格也没问题 |
| 首次启动 | 联网下载约 990 MB，国内常失败 | **直接可用**，不下载任何东西 |
| 启动方式 | 命令行 `python app.py`，或双击后闪退 | **双击「启动.bat」**，浏览器自动打开 |
| 换台电脑 | 整套环境得重来一遍 | **复制文件夹过去就能用** |

---

## 三、两个版本怎么选

它们其实是**同一个程序**（界面、功能、模型完全一样），唯一的差别是"用谁来算"：一个是电脑的 CPU，一个是 NVIDIA 显卡。

### app —— CPU 通用版（解压后约 1.24 GB）

- **靠什么算**：处理器（CPU）
- **硬件要求**：64 位 Windows，没有其他要求
- **速度**：约 9.6 个音符/秒
- **适合**：没有独立显卡的电脑、要拷给别人的场景

> 一句话：**哪台电脑都能跑，就是慢一点。**

### app-gpu —— 显卡加速版（解压后约 3.02 GB）

- **靠什么算**：NVIDIA 独立显卡（CUDA）
- **硬件要求**：**必须**是 NVIDIA 显卡，且驱动版本较新
- **速度**：约 48.3 个音符/秒，**约为 CPU 版的 5 倍**
- **适合**：有 N 卡的自己人自用、追求效率

> 一句话：**快 5 倍，但没 N 卡就跑不起来。**

### 实测速度对比（生成 64 个音符事件）

| 版本 | 耗时 | 速度 |
|---|---|---|
| app（CPU） | 6.70 秒 | 9.6 音符/秒 |
| app-gpu（显卡） | **1.33 秒** | **48.3 音符/秒** |

> 在 RTX 4060 Ti 上实测，同一台电脑、同一个模型、同样的参数。显卡版本快约 **5 倍**；生成的内容越长、一次要的方案越多，差距越大。

### 到底选哪个？

先开一个命令行窗口，敲 `nvidia-smi` 回车。

| 你看到的结果 | 选它 |
|---|---|
| 列出了显卡型号和 `CUDA Version: 12.x` 之类 | **app-gpu**，享受 5 倍速度 |
| 提示"不是内部或外部命令"、或报错 | **app**，稳妥好用 |
| 想拷给朋友的电脑用 / 到处带着走 | **app**，因为对方的电脑不一定有 N 卡 |

拿不定主意？**先下 app**。它一定能跑起来，用着觉得慢再换 app-gpu 也不迟。

---

## 四、三分钟用起来

不需要装任何东西，不需要命令行基础。

**第 1 步 · 解压到一个你喜欢的文件夹**
放 D 盘、E 盘、桌面都行，文件夹名字随便改，路径里有空格也没关系。建议不要放在 C 盘（省点系统盘空间）。

**第 2 步 · 双击文件夹里的「启动.bat」**
会出现一个黑色窗口，滚动一段文字后卡住不动——这是正常的，说明服务已经在运行。

```
----------------------------------------
   midi-model   便携版
   浏览器会自动打开：http://127.0.0.1:7860
   关闭这个黑窗口 = 关闭程序
----------------------------------------
```

**第 3 步 · 浏览器自动打开操作界面**
如果没有自动打开，自己开浏览器访问 `http://127.0.0.1:7860`。首次进入需要等模型加载，会有几十秒的等待。

**第 4 步 · 点生成，等结果**
在网页里选好乐器、速度、调式等，点生成按钮。等几秒到几十秒，页面上会出现乐谱图、试听音频和 MIDI 下载。

**第 5 步 · 去 outputs 文件夹拿成品**

```
outputs\
  output1.mid
  output2.mid
  ... 一直到
  output8.mid
```

> **注意**：文件名是固定的，下一次生成会覆盖上一次。满意的作品记得第一时间改名或另存到别处。

---

## 五、用之前必须知道的几件事

都是实测踩出来的，看完能省很多麻烦。

### 必须做到

**只能双击「启动.bat」，不要直接双击 app.exe。**
直接开 exe 会闪退。启动器负责把环境准备好再拉起程序，绕不过去。

**关掉那个黑色窗口，就是关掉程序。**
程序不会在后台偷偷驻留，也不会开机自启。想彻底退出，关黑窗口即可；浏览器页面可以直接叉掉，不影响。

### 需要注意

**第一次运行如果提示"无法建立目录链接"，改用管理员身份运行。**
程序需要在 C 盘建一个文件夹来"骗过"它内部写死的路径。多数情况下直接双击就行；万一 Windows 权限拦住了，右键 启动.bat → 以管理员身份运行，成功一次以后就正常了。

**C 盘会留下一个几 KB 的目录链接，这是正常的，也不是"安装"。**
位置是 `C:\tools\fluidsynth`。它只是个指路牌，指向你解压出来的文件夹，本身不占空间、不写注册表、不装运行库。删掉也没关系，下次启动会自动重建。但**程序运行期间需要它存在**。

**如果装了 360 之类带"C 盘搬家"的安全软件，可能干扰这个链接。**
实测发现这类软件会把 `C:\tools\fluidsynth` 悄悄挪到自己的目录下。如果遇到启动异常或链接反复失效，把程序所在文件夹加入安全软件白名单，或者临时退出它再启动。

### 可以放心

**全程不需要联网。**
模型和音色库已经打包在里面（约 990 MB），启动、生成、试听都在本地完成，不下载任何东西，也不上传你的音乐。断网也能用。

**文件夹可以随便改名、随便搬家。**
换盘符、改文件夹名、拷到 U 盘带到别的电脑，都不需要重新配置任何东西。只有启动器里一个自动维护的链接会跟着变，你什么都不用做。

**杀毒软件报警大概率是误报。**
这类用 PyInstaller 打包的程序被误报是常见现象。加信任即可。

---

## 六、常见问题

**Q：双击后黑窗口一闪就没了 / 什么都没发生？**
百分之九十是因为直接双击了 `app.exe`。请确认双击的是 **启动.bat**。如果确实是双击启动.bat 还是闪退，说明文件解压不完整，建议用 7-Zip 或 WinRAR 重新完整解压一遍。

**Q：黑窗口停住了，光标不闪，是不是卡死了？**
不是。模型加载和推理都是"闷着头干活"，中间没有输出。这时打开浏览器访问 `http://127.0.0.1:7860` 就能看到界面。首次启动、以及第一次生成时等待时间会明显长一些，之后就快了。

**Q：浏览器打不开 / 提示无法访问？**
先确认黑窗口还在（关掉就等于关程序了）。然后手动访问 `http://127.0.0.1:7860`。如果提示端口被占用，说明 7860 被别的程序占了，关掉它，或者一次只开一个副本。

**Q：显卡版启动报错 / 提示找不到 CUDA？**
显卡版要求 NVIDIA 显卡 + 较新的驱动。先去 NVIDIA 官网更新显卡驱动，重启后再试。如果显卡型号比较老（例如早期的 GT 系列、低端 MX 系列），可能本身就不被支持，这种情况请改用 CPU 版 **app**，功能完全一样，只是慢一些。

**Q：生成的曲子很短 / 听起来不完整？**
默认只生成 2048 个音符事件，这是长度限制，可以在界面里把"generate max n midi events"调大。另外一次会出 8 个方案，质量参差不齐是正常的——挑好的用就行。也可以把「接着上次」用起来，一段一段往下接。

**Q：两个版本能同时开着吗？**
技术上可以，但它们共用同一个 C 盘链接，链接只会指向"最后一次启动"的那个副本。因为底层依赖完全相同，不会出问题。不过为了省心、也为了少占内存和显存，建议一次只开一个。

**Q：能删掉包里的东西给它"瘦身"吗？**
- 不要删 `_internal` 文件夹，那里是程序本体，删了就再也打不开了。
- `models` 文件夹和 `soundfont.sf2` 是模型与音色库（约 990 MB）。删掉后程序会尝试联网重新下载，国内网络下大概率失败，也别删。
- `outputs` 文件夹里是你自己生成的作品，可以随时清理。

---

## 七、配置要求

| 项目 | 要求 |
|---|---|
| 系统 | 64 位 Windows（Windows 10 / 11 均可） |
| 显卡 | CPU 版：无要求；显卡版：NVIDIA 显卡 + 较新的驱动 |
| 磁盘空间 | CPU 版约 1.24 GB，显卡版约 3.02 GB（解压后） |
| 内存 | 建议 8 GB 以上 |
| 网络 | 不需要。首次使用、后续使用都不需要联网 |

---

## 关于这个包

本便携版基于开源项目 **Midi-Model**（作者 SkyTNT）的官方 Windows 发布包改造而成。
程序本身的版权与许可遵循原项目，请遵守原作者的授权条款；本包仅做了便携化封装，未修改程序功能。

**改造内容**：内置 fluidsynth 运行库 · 自愈式启动器（解决硬编码路径与搬迁问题）· 预置模型与音色库（离线可用）· 清理冗余依赖

遇到问题请先看上面的常见问题；仍未解决，请附上黑色窗口里的完整文字反馈。
